(function(){
  $('.catalog__list').slick({
    prevArrow: $('.catalog__arrow--prev'),
    nextArrow: $('.catalog__arrow--next'),
    speed: 500,
    slidesToShow: 1,
    // centerMode: true
  });
})();